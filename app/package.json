{
  "name": "infeed-app",
  "version": "1.0.0",
  "description": "Infeed — TikTok-style knowledge feed (Capacitor Android wrapper)",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "build:android": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/share": "^6.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
